// Deprecated compatibility wrapper - re-export followController
// Kept only for backward compatibility while codebase transitions to "follow*" naming.
module.exports = require('./followController');
